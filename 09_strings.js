a = "m s"
console.log(a, a.length)

//! ForOf loop -->
for (const char of a) {
    console.log(char)
}

//! ForEach loop does'nt work in strings

console.log(a.toUpperCase())
// More functions like slice, lastindexof, split etc